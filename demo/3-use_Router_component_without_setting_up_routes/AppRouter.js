/**
 * @highlight 7-8
 */

import { createRouter } from 'react-router-min'

// routes is not required
const { Link, Main, Router } = createRouter({ config: { base: '/3' } })

export { Link, Main, Router }
